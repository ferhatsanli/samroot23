# Current bounded objective

When an unmodified SM-S911B B9 FZDP (or other B9) `abl.elf` / extracted LinuxLoader PE is supplied, map the counterparts of CXDF `0xC6ED0` and EZB6 `0xCA790`, compare both event-loop call sites and long-press route, then determine whether it is dynamically reachable or hard-disabled. Compare the matching Odin PE for EM-token mode behavior if available.

Do not flash, downgrade, patch, or install EM tokens. No meaningful further local B9 comparison is possible without that exact component.
